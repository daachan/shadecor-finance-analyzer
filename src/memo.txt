    # def isDatasetExists(folder_path):
    #     # マスタDBが存在しているか確認
    #     if not folder_path:
    #         return False

    #     masterdb_path = os.path.join(folder_path, "dataset.csv")
    #     return os.path.exists(masterdb_path)

    # def hasErrorFiles(folder_path):
    #     # 特定のデータファイルが存在するか確認
    #     if not folder_path or not os.path.exists(folder_path):
    #         return False

    #     datalist = os.listdir(folder_path)
    #     print(datalist)

    #     for data in datalist:
    #         fn, ex = os.path.splitext(data)
    #         print(ex)

    #     return len(datalist) > 0

    # def main(page: Page):
    #     page.title = "Example Layout"
    #     page.padding = 10

    #     # 環境変数の読み込み
    #     env_path = os.path.join(os.path.dirname(os.path.abspath(__file__)), ".env")
    #     load_dotenv(env_path)

    #     SCRIPT_FOLDER_PATH = os.environ.get("SCRIPT_FOLDER_PATH")
    #     ERROR_FOLDER_PATH = os.environ.get("ERROR_FOLDER_PATH")

    #     print(hasErrorFiles(ERROR_FOLDER_PATH))

    #     # 論理チェックを実行
    #     if not hasErrorFiles(ERROR_FOLDER_PATH) and not isDatasetExists(SCRIPT_FOLDER_PATH):
    #         page.add(Text("アプリケーションが正常に起動しました"))
    #         # チェックOKならレイアウトを表示
    #         # page.add(MyLayout(page))

    #     else:
    #         # チェックNGならエラーメッセージを表示
    #         page.add(Text("エラー：必要なファイルが見つからないため起動できません"))
